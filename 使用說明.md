# 川記麵線糊 LINE 點餐系統 · 第一版

## 已完成
- LINE 聊天中的滑動菜單卡片與按鈕點餐，無須 OpenAI API。
- 七款麵線、大小碗、七種加料、五款滷味、逐碗口味、辣度、香菜換九層塔。
- 同口味數量、購物車、移除重選、電話、日期時間、備註、金額確認。
- 週六公休，取餐 11:00–22:59，拒絕過去時間。23:00 視為打烊；可依實際規則調整。
- SQLite 儲存訂單與對話進度，LINE 事件去重、簽章驗證、店家通知重試。
- 店家收到完整訂單，客人顯示「等待店家確認」。目前不是自動接單承諾。
- 本機試點畫面，使用與 LINE 相同的訂單規則；不傳真實 LINE 通知、不收款。

## 試用
需要 Python 3.10 以上，無額外套件。

    python server.py --demo

瀏覽器開啟 http://127.0.0.1:8787 。每個瀏覽器的試用訂單分開保存。
可在預覽內走完流程，在右側查看模擬店家通知。
正式 LINE 畫面由 LINE 呈現，外觀與本機預覽不同。

## 目前尚未上線
程式已完成首版，但尚未部署，也未連接你的 LINE 官方帳號。
請不要把 localhost 網址提供給客人；其他人無法透過它點餐。

## 正式 LINE 串接
1. 在提供 HTTPS 與持久儲存空間的主機部署此資料夾，使用 Python 3.10+。
2. 在主機的安全環境變數設定 LINE_CHANNEL_SECRET、LINE_CHANNEL_ACCESS_TOKEN、LINE_OWNER_USER_ID。
   owner 必須是店家接通知的個人 LINE user ID，並已加官方帳號好友。不是商店 ID，也不是 LINE Pay ID。
   勿將任何密鑰貼到聊天、版本庫或網頁。
3. ORDER_DB 設成持久磁碟上的完整路徑。單一程序執行，不可多副本共用 SQLite。
4. 啟動 python server.py；由平台以 HTTPS 反向代理連到 PORT（預設 8787）。
5. LINE Developers Messaging API 的 Webhook URL 填 https://你的主機/webhook ，驗證並啟用 webhook 與重新傳送。
6. 配合原帳號設定，檢查原自動回應是否會與點餐回覆衝突。
7. 客人在一對一聊天室輸入「點餐」「開始點餐」或「菜單」即可開始。群組不接受訂單。
8. 先由店家測試一筆，確認客人回覆與店家推播都收到，再向客人開放。

健康檢查：GET /health。正式模式不提供預覽或公開訂單查詢介面。

## 上線前待確認
- 取餐地址（尚未提供），應補入客人確認訊息。
- 最短備餐時間、每日數量與售完控制、最遠可預約天數。目前只限制營業時間與非過去日期。
- 店家如何人工確認接單、聯繫客人；首版提供電話聯繫與 LINE 通知，不含接單／完成按鈕。
- 主機、LINE 推播額度及實際費用，由帳號方案決定。
- 資料保存期間與備份。資料庫含電話，應限制主機與備份的存取權限。
- 需監控服務日誌；通知多次失敗會保留在 outbox（sent=-1），需修復設定後人工重試。
- 收到訂單但尚未付款，不能視為已付款。付款模組尚未接入，無 LINE Pay 密鑰需求。

## 線上付款後续
orders.data 預留 payment='unpaid'。未來新增付款請求、付款確認及退款流程；金額以伺服器重算為準。只有金流確認成功才能標示已付款，不信任客人返回頁或截圖。

## 測試
    python -m unittest -v test_orders.py

## 範圍
這是獨立實作、操作方式類似 LINE 點餐服務的首版，與「快一點」沒有隸屬或合作關係。
每筆最多10項餐點、每項1–6份、每碗最多6份加料，避免訊息過長；超量需分單或聯絡店家。
調味及加料套用該項所有碗，不同口味要分次加入。備註由店家人工判斷，不會自動改價。
